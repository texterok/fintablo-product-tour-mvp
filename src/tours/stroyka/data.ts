// ============================================================================
// ROMA EDITABLE FILE
// ============================================================================
//
// Здесь живут демо-цифры для тура «Стройка» — выручка, прибыль, статьи,
// объекты. Эти цифры показываются клиенту в туре.
//
// Что можно менять:
//   ✓ значения (числа, проценты, имена компаний и объектов)
//   ✓ комментарии справа от цифр
//
// Что НЕЛЬЗЯ менять:
//   ✗ имена exported констант (например `dealsList`) — на них ссылаются
//     компоненты тура. Переименуешь — тур сломается.
//   ✗ структуру объектов (если у объекта поле `plan: number`, оставь
//     `plan: number` — нельзя поменять на `plan: string` или удалить).
//
// Файл сейчас просто реэкспортирует данные из старой локации
// (src/lib/data.ts). Когда понадобится сильно менять — Иван перенесёт
// сюда содержимое целиком.
// ============================================================================

export * from "@/lib/data";
