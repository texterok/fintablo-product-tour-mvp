// ============================================================================
// ROMA EDITABLE FILE
// ============================================================================
//
// Тексты лендинга «Стройка» (страница / на сайте).
//
// Что можно менять:
//   ✓ любую строку в этом файле — заголовки, подзаголовки, кнопки, описания
//
// Что НЕЛЬЗЯ менять:
//   ✗ имена exported констант (например `hero.h1`) — на них ссылается
//     StroykaLanding.tsx компонент. Переименуешь — лендинг сломается.
//
// Сейчас copy живёт прямо внутри `StroykaLanding.tsx` (component) —
// этот файл подготовлен на вырост. Когда Ивана попросишь
// «вынести texts отдельно для удобства» — он перенесёт их сюда.
// ============================================================================

// Placeholder: copy is still inlined in StroykaLanding.tsx for now.
// This file exists so the Roma-boundary CI check knows where copy
// lives once it's externalized.

export const stroykaCopy = {
  // Future shape:
  // hero: { eyebrow: "...", h1: "...", sub: "...", primaryCta: "..." },
  // pain: { eyebrow: "...", h2: "...", items: [...] },
  // ...
} as const;
